# Validates repo standards and checks markdown.
# Called by ci.yml
name: Repo Standard

on:
  workflow_call:

jobs:
  validate:
    name: Validate
    uses: ai-cfia/github-workflows/.github/workflows/workflow-repo-standards-validation.yml@main
    secrets: inherit

  markdown-check:
    name: Markdown Check
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: DavidAnson/markdownlint-cli2-action@07035fd053f7be764496c0f8d8f9f41f98305101 # v22
        with:
          globs: "**/*.md"
      - uses: tcort/github-action-markdown-link-check@v1
